export type NavItem = {
  label: string;
  href: string;
};

export type ProgrammeStage = {
  label: string;
  title: string;
  items: string[];
};

export type FaqItem = {
  question: string;
  answer: string;
};

export type DeliveryAllocation = {
  label: string;
  value: string;
  width: string;
};
